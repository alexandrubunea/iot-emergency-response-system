import { DeviceLog } from "../types/DeviceLog";

type LogRowProps = {
    log: DeviceLog;
};

function LogRow({log}: LogRowProps) {
    return (
        <>
        </>
    );
}

export default LogRow;
